---
"element-legacy": minor
"@element-legacy/theme-chalk": minor
---

Add a `singleClear` prop to DatePicker v2 that defaults to a single clear button clearing the full range, with an opt-out to keep two clear buttons.
